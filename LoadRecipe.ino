void LoadRecipe()
{
			//Inserire lo scan dei file dentro SD card
			//NOME_RICETTA.toCharArray(charbuf,50);
			//========================================================
			testGetline();
			TMush_IN          =   ( mTMush_IN         * 1000  * 60);
			TAcid_Rest        =   ( mTAcid_Rest       * 1000  * 60);
			TProtein_Rest     =   ( mTProtein_Rest    * 1000  * 60);
			TB_amilasi        =   ( mTB_amilasi       * 1000  * 60);
			TA_amilasi        =   ( mTA_amilasi       * 1000  * 60);
			TS_unica_AB       =   ( mTS_unica_AB      * 1000  * 60);
			TMush_OUT         =   ( mTMush_OUT        * 1000  * 60);
			TBoil             =   ( mTBoil            * 1000  * 60);
			Tluppoli1         =   ( mTluppoli1        * 1000  * 60);
			Tluppoli2         =   ( mTluppoli2        * 1000  * 60);
			Tluppoli3         =   ( mTluppoli3        * 1000  * 60);
			TCorrezioneSosta  =   ( mTCorrezioneSosta * 1000  * 60);
			/*
			const char *const dastamp[ultimo] = {"Mu. in:","So. AB:","M. out:"};//,"Acid r:","Prot r:","So. AB:","M. out:"};//NomiSteps[Steps]
			int stampo;
			unsigned long Tempo []={mTMush_IN,mTS_unica_AB,mTMush_OUT}; //mTAcid_Rest,mTProtein_Rest,mTS_unica_AB,mTMush_OUT};
			unsigned long indiceTempo;
			unsigned long T = 0;
			float gradi []={tMush_IN,tS_unica_AB,tMush_OUT}; //tAcid_Rest,tProtein_Rest,tS_unica_AB,tMush_OUT};
			int indiceTemperature;
			float t = 0;
			*/
}
